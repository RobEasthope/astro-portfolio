---
import type { Render } from "astro:content";

interface Props {
  markdown: { data: { title: string }; render(): Render[".mdx"] };
}

const { Content } = await Astro.props.markdown.render();
---

<div>
  <Content />
</div>
